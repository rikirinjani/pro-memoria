"""Pro Memoria benchmark scripts.

The actual benchmark code lives in the root-level `bench/` directory.
This package marker enables `pip install -e .` discovery.

Run benchmarks from the repo root:
    python -m bench.track_efficiency
    python -m bench.token_efficiency
"""
