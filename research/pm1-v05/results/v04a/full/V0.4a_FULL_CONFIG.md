# V0.4a Full Experiment — Recorded Preflight Configuration

- Date: 2026-08-16
- Purpose: authorized 4,000-call full experiment
- Runner: `run_v04a_full.py` (executed unchanged except output destination —
  see note below)
- Results dir: `results/v0.4a/full/` (new dedicated location; all prior
  artifacts preserved untouched)

## Model / API

- Model: deepseek-v4-flash
- Endpoint: https://api.deepseek.com/v1
- Temperature: 0.0
- Max tokens: 2048
- Max retries: 3
- API key: read from `.llm_creds.json` by `get_model_config()` (never persisted)

## Experiment Matrix

- 10 chains x 100 hops x 4 conditions = **4,000 API calls**
- Conditions: H-full, H-direct, H-corrupt, H-recover
- Chain pool: chain-01 .. chain-10 (deterministic, base_seed=42)
  - covers HOLD/BUY/SELL-producing states, zero/positive/negative positions,
    multi-unit differences, target changes
- Fault schedules: per-chain deterministic corrupt_hops / restore_hops

## Preflight Verification

| Check | Result |
|---|---|
| Call count | 10 x 100 x 4 = 4,000 |
| Conditions | H-full, H-direct, H-corrupt, H-recover |
| Model | deepseek-v4-flash, temp 0.0, max_tokens 2048 |
| Prompt isolation | state repr + task spec only (test-verified, no chain/hop/seed/expected/condition) |
| Oracle independence | worker modules do not import oracle (test-verified) |
| F1 gating | corruption only H-corrupt/H-recover; restore only H-recover (test-verified) |
| F2 tokens | real usage captured with usage_available flag (test-verified) |
| F3 persistence | per-hop records persisted, no secrets (test-verified) |
| F4 observability | chain-06 multi-unit (test-verified) |

## Note on the Output-Destination Change

The revalidation-phase `run_v04a_full.py` still pointed `RESULTS_DIR` at
`results/v0.4a/revalidation/`. Per the authorization instruction, full
experiment outputs must go to a NEW location with mandated artifact names
(`V0.4a_FULL_CONFIG.md`, `V0.4a_FULL_REPORT.md`, `v04a_full_results.json`).
The runner's output destination and artifact filenames were updated to
`results/v0.4a/full/`. **No benchmark logic was changed** — chains, hops,
conditions, model, worker, oracle, prompts, fault schedules, and gating are
identical to the validated revalidation configuration.

## Acceptance Criteria (pre-registered, unchanged)

- H-full 100-hop chain survival >= 70%
- H-full handoff success >= 95%
- H-full state integrity (divergence==0) >= 95%
- H-full vs H-direct equivalence within +/- 5 pp
- Long-horizon drift: trend not increasing/catastrophic
- Corruption propagation (H-corrupt): >= 90% of corrupt hops show divergence != 0
- Recovery (H-recover): >= 70% of corrupted chains recover
- Zero metadata leakage, zero oracle leakage, reproducibility
